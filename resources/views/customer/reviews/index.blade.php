@extends('layouts.customer.app')
@section('title', 'My Reviews - Tokobii')
@section('content')
<div class="container-fluid px-0"><h2 class="fw-bold mb-1">My Reviews</h2><p class="text-muted mb-4">Riwayat review produk Anda.</p>@forelse($reviews as $review)<div class="card border-0 shadow-sm mb-3"><div class="card-body"><h5 class="fw-bold">{{ $review->product->name }}</h5><div class="text-warning mb-2">{{ str_repeat('★', $review->rating) }}{{ str_repeat('☆', 5 - $review->rating) }}</div><p>{{ $review->comment }}</p><small class="text-muted">Order: {{ $review->order->invoice_number }} · {{ $review->created_at->format('d M Y') }}</small><div class="mt-3"><a class="btn btn-sm btn-outline-primary" href="{{ route('customer.shop.show', $review->product->slug) }}">Lihat Produk</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('customer.reviews.edit', $review) }}">Edit Review</a></div></div></div>@empty<div class="card border-0 shadow-sm"><div class="card-body text-center py-5"><h5>Belum ada review.</h5><a class="btn btn-primary" href="{{ route('customer.orders.index') }}">Lihat Pesanan</a></div></div>@endforelse{{ $reviews->links('pagination::bootstrap-5') }}</div>
@endsection
